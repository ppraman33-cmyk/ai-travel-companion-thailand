# Synthetic test fixtures

Everything in this folder is invented and must remain restricted to automated tests and local
development. Fixtures must use explicit `synthetic-` identifiers and unmistakable labels.

These records are not verified real-world facts and must never be imported into a production
publication workflow.
